extends layout

include mixins/displayStore
include mixins/pagination

block content
  .inner
    h2=title
    .stores
      each store in stores
        +displayStore(store)
    +pagination(page, pages, storeCount)